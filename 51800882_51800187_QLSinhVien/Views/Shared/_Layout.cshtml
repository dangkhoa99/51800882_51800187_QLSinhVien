<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width" />
    <title>@ViewBag.Title</title>
    @Styles.Render("~/Content/css")
    @Scripts.Render("~/bundles/modernizr")
</head>
<body>
    <div class="navbar navbar-inverse navbar-fixed-top">
        <div class="container">
            <div class="navbar-header">
                <button type="button" class="navbar-toggle" data-toggle="collapse" data-target=".navbar-collapse">
                    <span class="icon-bar"></span>
                    <span class="icon-bar"></span>
                    <span class="icon-bar"></span>
                </button>
                @Html.ActionLink(@Resources.Content.systemManagementStudent, "Index", "Home", new { area = "" }, new { @class = "navbar-brand" })
            </div>
            <div class="navbar-collapse collapse">
                @*<ul class="nav navbar-nav">
                        <li>@Html.ActionLink("Quản lý Khoa", "Index", "Khoa", new { area = "" }, null)</li>
                        <li>@Html.ActionLink("Quản lý Sinh Viên", "Index", "SinhVien", new { area = "" }, null)</li>
                        <li>@Html.ActionLink("Quản lý Môn Học", "Index", "MonHoc", new { area = "" }, null)</li>
                        <li>@Html.ActionLink("Quản lý Điểm", "Index", "KetQua", new { area = "" }, null)</li>
                    </ul>*@
                <div style="display: flex; align-items: center; height: 50px; justify-content:flex-end">
                    @if (User.Identity.IsAuthenticated)
                    {
                        <a style="padding-right: 20px; color: #fff; text-decoration:none">@Resources.Content.hello, <b>@User.Identity.Name!</b></a>

                        using (Html.BeginForm("Logout", "Home", FormMethod.Post))
                        {
                            <a class="btn btn-danger" href="javascript:;" onclick="document.forms[0].submit();">@Resources.Content.logout</a>
                        }
                    }
                    <div style="display: flex; justify-content: flex-end; margin-right: 20px;">
                        <a style="margin-left: 10px" href="@Url.Action("ChangeLanguage", "Home", new { lang = "en" })"><img src="https://cdn.countryflags.com/thumbs/united-states-of-america/flag-800.png" width="40" height="26" /></a>
                        <a style="margin-left: 10px" href="@Url.Action("ChangeLanguage", "Home", new { lang = "vi" })"><img src="https://cdn.countryflags.com/thumbs/vietnam/flag-400.png" width="40" height="26" /></a>
                    </div>
                </div>
            </div>
        </div>
    </div>
    <div class="container body-content">
        @RenderBody()
        <hr />
        <footer>
            <p>&copy; @DateTime.Now.Year - 51800882 - 51800187 - QLSV</p>
        </footer>
    </div>

    @Scripts.Render("~/bundles/jquery")
    @Scripts.Render("~/bundles/bootstrap")
    @RenderSection("scripts", required: false)
</body>
</html>
